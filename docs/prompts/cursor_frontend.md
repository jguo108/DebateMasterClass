
I’ve given you a folder that contains all the static code and page screenshots of a web application I want to build. I’ve also given you the PRD for this project. 

Now, please create a high-fidelity prototype for me (no backend and no database, pure frontend code):
* Make sure you implement all pages
* Make sure the fonts, colors, layouts, vibe are exactly the same as the pages provided
* Make sure the app can be shown properly on both desktop and mobile
* Use Next.js, React, Typescript, Tailwind CSS, Motion, Lucide React
* Only use mock data to simulate the system's behaviour 
* Make sure the code generated can be easily switched to a real backend (Vercel + Supabase)
